<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.infotsav.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenuUtils.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main_Activities.HomeActivity" />
        <activity android:name=".Main_Activities.EventsActivity" />
        <activity android:name=".Main_Activities.RegisterActivity" />
        <activity android:name=".Main_Activities.UpdatesActivity" />
        <activity android:name=".Main_Activities.ContactusActivity" />
        <activity android:name=".Main_Activities.SponsersActivity" />
        <activity android:name=".foldingView.FoldingActivity" />
        <activity android:name=".Main_Activities.TreasurehuntActivity"></activity>
        <activity android:name=".Main_Activities.AboutusActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.CALL_PHONE"/>

</manifest>